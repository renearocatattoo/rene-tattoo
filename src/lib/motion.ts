/** Duração padrão das animações (segundos). */
export const MOTION_DURATION = 0.55;

/** Distância padrão de deslocamento (px). */
export const MOTION_OFFSET = 20;

/** Intervalo entre itens em listas staggered (segundos). */
export const MOTION_STAGGER = 0.08;

/** Margem do observer para disparar antes do elemento entrar totalmente. */
export const MOTION_VIEW_MARGIN = "0px 0px -40px 0px";

/** Threshold mínimo de visibilidade para disparar animação. */
export const MOTION_VIEW_THRESHOLD = 0.12;
